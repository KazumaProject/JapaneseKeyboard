<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/shortcut_toolbar_add_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="?attr/colorSurface"
    app:cardCornerRadius="8dp"
    app:cardElevation="0dp"
    app:strokeColor="?attr/colorOutline"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="76dp"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="10dp"
        android:paddingBottom="10dp">

        <FrameLayout
            android:id="@+id/shortcut_toolbar_add_icon_container"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/shortcut_toolbar_icon_background"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/shortcut_toolbar_add_icon"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_gravity="center"
                android:tint="?attr/colorPrimary"
                tools:src="@drawable/keyboard_floating_24px" />

        </FrameLayout>

        <TextView
            android:id="@+id/shortcut_toolbar_add_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnSurface"
            app:layout_constraintBottom_toTopOf="@id/shortcut_toolbar_add_summary"
            app:layout_constraintEnd_toStartOf="@id/shortcut_toolbar_add_button"
            app:layout_constraintStart_toEndOf="@id/shortcut_toolbar_add_icon_container"
            app:layout_constraintTop_toTopOf="@id/shortcut_toolbar_add_icon_container"
            app:layout_constraintVertical_chainStyle="packed"
            tools:text="フローティング切替" />

        <TextView
            android:id="@+id/shortcut_toolbar_add_summary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintBottom_toBottomOf="@id/shortcut_toolbar_add_icon_container"
            app:layout_constraintEnd_toStartOf="@id/shortcut_toolbar_add_button"
            app:layout_constraintStart_toEndOf="@id/shortcut_toolbar_add_icon_container"
            app:layout_constraintTop_toBottomOf="@id/shortcut_toolbar_add_title"
            tools:text="フローティングモードを切り替えます" />

        <ImageButton
            android:id="@+id/shortcut_toolbar_add_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/shortcut_toolbar_add"
            android:padding="8dp"
            android:scaleType="center"
            android:src="@drawable/baseline_add_circle_outline_24"
            android:tint="?attr/colorPrimary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
